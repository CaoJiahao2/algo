# Minimum Window Substring

## 题目
给定字符串 `s` 和 `t`，返回 `s` 中涵盖 `t` 所有字符的最小子串；不存在则返回空串。

## 思路
滑动窗口 + 频次表：右指针扩展，当窗口覆盖 `t` 后收缩左指针并更新最短窗口。

## 复杂度
- 时间：O(|s| + |t|)
- 空间：O(字符集大小)

## 代码
见 [solution.py](solution.py)

