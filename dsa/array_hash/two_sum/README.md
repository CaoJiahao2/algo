# Two Sum

## 题目
给定整数数组 `nums` 和目标值 `target`，返回和为 `target` 的两个数的下标。假设每个输入恰有一组答案，且同一元素不能使用两次。

## 思路
一次遍历，用哈希表记录已遍历元素的值到下标；对当前元素 `x`，查找 `target - x` 是否已在表中。

## 复杂度
- 时间：O(n)
- 空间：O(n)

## 代码
见 [solution.py](solution.py)

